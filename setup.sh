#!/bin/bash

cp -r maxtest.test ~/.config
cp -r fontforge ~/.config
cp -r kitty ~/.config
cp -r nvim ~/.config
cp -r ohmyposh ~/.config
cp -r swappy ~/.config
cp -r swaync ~/.config
cp -r wallust ~/.config
cp -r waybar ~/.config
